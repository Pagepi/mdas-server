spring.application.name=mdas-server

# 下面是需要追加的数据库配置
# 应用服务器端口
server.port=8080

# 数据库连接配置 - 指向您的Ubuntu虚拟机
spring.datasource.url=jdbc:postgresql://192.168.1.10:5432/mdas
spring.datasource.username=mdassa
spring.datasource.password=flzx3qcYSYHL(T

# JPA 和 Hibernate 配置
spring.jpa.database=POSTGRESQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=mdas-dev-secret-2024-spring-boot-3-jwt-key-for-authentication
jwt.expiration=604800000
jwt.issuer=mdas-server

# 登录配置
login.max-fail-count=5
login.lock-duration=1800

# 可选：在控制台输出彩色的SQL日志，更易读
logging.pattern.console=%clr(%d{HH:mm:ss.SSS}){faint} %clr(%-5level) %clr(%logger{36}){cyan} %clr(:){faint} %m%n